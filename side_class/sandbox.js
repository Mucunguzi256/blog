// Loops
for (let i = 0; i < 5; i++) {
  consele.log('in loop' + i);
}
const animals = ['hen', 'cow', 'dog', 'sheep'];
let i = 0;
while (i < animals.length) {
  console.log(animals[1]);
  i++;
}

// logical operators OR || & AND &&